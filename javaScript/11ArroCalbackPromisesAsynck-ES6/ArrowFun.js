// Traditional function expression
const traditionalFunction = function(param) {
    return param * 2;
};

// Arrow function expression
const arrowFunction = (param) => {
    return param * 2;
};

// Even more concise arrow function (when only a single expression is returned)
const conciseArrowFunction = param => param * 2;
